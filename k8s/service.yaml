apiVersion: v1
kind: Service
metadata:
  name: fiscal-ecuador-node
  labels:
    app: fiscal-ecuador-node
spec:
  type: NodePort
  selector:
    app: fiscal-ecuador-node
  ports:
    - protocol: TCP
      port: 3010
      targetPort: 3010
